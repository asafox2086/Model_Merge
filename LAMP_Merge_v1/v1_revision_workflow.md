# LAMP-Merge v1 Revision Workflow

本文件记录 v1 论文修缮的执行顺序。每完成一项，先检查该项是否满足要求，并确认此前已完成项没有被破坏，再进入下一项。

1. [x] 获取并定位用户新写的 v1 版本，确认编辑入口、图片目录和编译入口。
2. [x] 将全文所有“事后模型融合”统一改为“模型融合”，并检查是否误伤引用名或术语。
3. [x] 压缩 Related Work 到约原长度的一半，采用“已有方法是什么；however；为什么不适用于本文场景”的结构，突出问题并增加必要引用。
4. [x] Experiment 部分分点介绍 datasets、baselines、settings 和其他实验细节。
5. [x] 美化主结果表：非数据表头行用灰色，LAMP-Merge 行用浅黄色，baseline 名称旁加入论文引用；在 K=3、K=5、K=7 后增加三者平均列 Avg，并为 Avg 列上色。
6. [x] 将表格中的 Variable setting 移到表外，用 itemized 文本逐项解释。
7. [x] 重画或美化超参数图，提升字体大小，统一配色、标记形状和论文风格。
8. [x] 将 “Dataset-level client-average accuracy in the module ablation” 表改为图。
9. [x] 调整 t-SNE 图：增大字体；使用 joint 对比图；单行展示；不再放单独的 LAMP-only 图。
10. [x] 调整版面位置：intro 图放第一页右上角，method 图放第二页横向展示。
11. [x] Dataset statistic 不再使用统计表，改为若干示例图像。
12. [x] 给主要表格统一上色，并保持图、表颜色和形状一致；最终编译 PDF 检查，确认排版后提交并 push。

## 2026-07-12 图表排版二次修订

本轮只做排版、图像重绘与最小必要的正文图表引用补充，不改变实验数值。

1. [x] 将数据集示例图提前一页，使其更靠近 Evaluation Datasets 段落。
2. [x] 将主结果表中的浅黄色、灰色、蓝色底色改为连续行/连续列视觉效果，避免每个单元格被分割；同时保证加粗数字在浅黄色底色上仍然清晰。
3. [x] 降低 Table 7 的字号，避免该表相对正文过大。
4. [x] 放大 Fig.4 与超参数图的底色框和边距，避免内容贴边或溢出。
5. [x] 删除超参数图中 peak 相关标注，不再叙述 peak。
6. [x] 放大 Fig.6 的字体和图形，必要时改为横向双栏图。
7. [x] 重绘 Fig.7，使预测分布图更符合论文图形风格。
8. [x] 放大 Fig.8 与 Fig.9 的图内字体；必要时采用横向双栏展示。
9. [x] 检查实验正文中的图表引用，补充“结果如 Fig.~X 所示”等对应关系。
10. [x] 后续所有图中统一用黄色表示 LAMP-Merge，与主表配色保持一致。
11. [x] 给主表中的 \texttt{avg} 和 \texttt{from} 补充论文引用，并确认引用对象与方法含义一致。
12. [x] 实验设置部分不再单独加入 ACC 指标公式说明和超参数设置，只保留数据集、baseline、融合流程与运行环境等必要实验信息。
13. [x] 非主表表格增加变化量表达，并用绿色上升箭头、红色下降箭头标注相对变化；主结果表不做该处理。
14. [x] 检查并调整图表浮动顺序，确保论文中表格和图片编号尽量按正文叙述从前到后递增。
15. [x] 重画超参数分析图：每个模块包含多条超参数曲线，并在正文中说明这些超参数变化对性能的影响。
16. [x] 重新编译 PDF，逐页抽查图表位置、字体、配色和浮动顺序。
17. [x] 提交并 push 当前分支。

## 2026-07-12 图表排版三次修订

本轮继续只做论文排版、表格样式和图像重绘，不改变实验数值。

1. [x] 将数据集样图放到上一页；样图中的 class 0/1 标签移到原图下方，避免遮挡图像内容。
2. [x] 修复主表灰色表头断开的问题，使表头底色视觉上连续。
3. [x] 增大 LAMP-Merge 黄色高亮行的上下高度，并增强黄色底色上加粗数字的可见度。
4. [x] 重画超参数图：移除 Blood 与 Ultrasound 曲线，增大中间间距，并缩短图表上方文字与图之间的距离。
5. [x] 调整 Table 8 中 LAMP-Merge 黄色单元格的左右宽度，使其与第一行方法名宽度对齐。
6. [x] 调整 Fig.6 右下角图例，使说明文字分两行展示。
7. [x] 重画 Fig.7，去掉 LAMP-Merge 行在分布图中的底色。
8. [x] 将 t-SNE 图设置为包含 LAMP-Merge 在内的四张基线算法对比图。
9. [x] 重画 Fig.4，去掉黄色背景带。
10. [x] 放大 Fig.8 和 Fig.10 的图中文字。
11. [x] 将 Fig.10 设为横跨整页的双栏图，并放大内部文字。
12. [x] 编译 PDF 并逐页检查上述修改；提交并 push 当前分支。

## 2026-07-12 图表排版四次修订

本轮仅调整版面、表格样式、图像尺寸和浮动位置，不修改正文、中文注释或实验数值。每完成一项，回读本清单并检查先前项目未被破坏。

1. [x] 调整主表边界：Method 灰色表头底边与其下方横线平齐；\texttt{avg} 蓝色单元格顶边与表头上方横线平齐。
2. [x] 重画超参数图，为各条参数曲线使用可区分的三角形、方形、圆形等标记。
3. [x] 调整 Table~8：LAMP-Merge 黄色高亮不覆盖上方横线，右侧变化箭头按列垂直对齐。
4. [x] 重排并缩小 t-SNE：单行 1x4，图内文字接近正文大小，并改为 LAMP-Merge 与基线方法的对比。
5. [x] 微调 Fig.~9：适当减小标题和右侧说明文字，消除其与左侧图形的重叠。
6. [x] 前移图表浮动体，避免正文前段空置、图表集中堆积在参考文献之前；编译逐页检查并提交 push。

## 2026-07-12 图表排版五次复核

本轮仅调整版面、表格样式、图像尺寸和浮动位置，不修改正文、中文注释或实验数值。每完成一项，回读本清单并检查先前项目未被破坏。

1. [x] 复核并微调主表边界：`Method` 灰色表头底边与其下方横线平齐；`avg` 蓝色单元格顶边与表头上方横线平齐。
2. [x] 复核超参数图的多曲线标记，为各条参数曲线保留可区分的圆形、三角形、方形与菱形标记。
3. [x] 复核 Table~8：LAMP-Merge 黄色高亮不覆盖上方横线，右侧变化箭头按列垂直对齐。
4. [x] 复核 t-SNE 与 Fig.~9：t-SNE 采用横向 1$\\times$4 的 LAMP-Merge 与基线对比，图内字体接近正文字号；适当减小 Fig.~9 标题与右侧说明文字，避免重叠。
5. [x] 复核图表浮动位置，将可放置的图表前移，避免正文前段空置、图表堆积在参考文献之前；重新编译并逐页检查。
