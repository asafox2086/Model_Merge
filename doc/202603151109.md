# 2026-03-15 11:09 更新记录

记录区间：2026-03-14 至 2026-03-15 11:09

## 这段时间完成的工作

1. 搭好了 `model_hub -> merge -> evaluate -> reports` 的下游闭环。
2. 完成了 `avg` 的全量 batch 融合与测试，结果保存在 `outputs/all_avg_full_20260314/`。
3. 将 `TIES / DARE-Linear / DARE-TIES / RegMean / Fisher` 接入到统一的 `methods/` 接口中。
4. 将 `RegMean` 和 `Fisher` 的统计量统一改成从 `.npz` 的 `val` split 提取。
5. 修掉了之前批量脚本里的旧队列残留和循环导入问题。
6. 新增了官方实现风格的 targeted smoke，并在评估后删除 `merged.pt`，只保留结果。
7. 新建 `doc/` 目录，用于记录仓库目标、每日更新和实验进度。

## 这次代码层面的关键调整

- `TIES` 改为 task vector 扁平化、trim、elect sign、disjoint merge 的官方核心流程。
- `DARE-Linear` 改为随机裁剪并重缩放 task vector 后再做线性融合。
- `DARE-TIES` 改为随机裁剪并重缩放后，再走 TIES 的核心流程。
- `RegMean` 改为基于线性层输入协方差的矩阵融合，并支持 `reduce_non_diagonal_ratio`。
- `Fisher` 改为基于预测分布的 diagonal Fisher，并支持官方式的 fisher norm 归一化。

## 2026-03-15 这轮 targeted smoke 结果

| task | dataset | model | method | test_acc | test_loss |
|---|---|---|---:|---:|---:|
| small | bloodmnist_224 | resnet | dare_linear | 0.2733 | 1.8805 |
| small | bloodmnist_224 | resnet | dare_ties | 0.3671 | 1.7364 |
| small | bloodmnist_224 | resnet | fisher | 0.3481 | 1.7869 |
| small | bloodmnist_224 | resnet | regmean | 0.3309 | 1.8244 |
| small | bloodmnist_224 | resnet | ties | 0.2122 | 1.9294 |
| vlm | bloodmnist_224 | openai/clip-vit-base-patch32 | fisher | 0.1692 | 2.8564 |
| vlm | bloodmnist_224 | openai/clip-vit-base-patch32 | regmean | 0.3122 | 2.4115 |
| vlm | bloodmnist_224 | openai/clip-vit-base-patch32 | ties | 0.2578 | 3.9199 |
